import React, { Component } from "react";

export default class InputComponents extends Component {
   render() {
      return (
         <div>
            Create 7 input fields with input types of your choice. then practice
            what we've done today
         </div>
      );
   }
}
